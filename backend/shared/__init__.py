"""Shared hexagonal ports and logging protocol.

Public symbols are intentionally minimal for Work Unit 1: the outbound
``Retrieve`` and ``Generate`` protocols plus a safe JSON logging protocol.
No persistence interface is exposed here by design.
"""
